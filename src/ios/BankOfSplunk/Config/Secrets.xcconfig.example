// Copy to Secrets.xcconfig and fill in your Splunk RUM credentials.
// Secrets.xcconfig is gitignored — never commit real tokens.
//
// Debug.xcconfig and Release.xcconfig include this file and do not override
// RUM_* keys, so Observability Cloud settings are controlled here only.

API_BASE_URL = http:/$()/127.0.0.1:8083
RUM_REALM = us0
RUM_ACCESS_TOKEN = disabled
RUM_APP_NAME = bank-of-splunk-ios
RUM_ENVIRONMENT = bank-local
SPLUNK_VERSION = 1.0.0
