#include "Secrets.xcconfig"

// Build-flavor defaults only. Splunk RUM credentials come from Secrets.xcconfig.
// App version: set MARKETING_VERSION in Secrets.xcconfig or Xcode → General → Version.
// Do not override MARKETING_VERSION here — it becomes RUM resource attribute app.version.
API_BASE_URL = http:/$()/127.0.0.1:8083
